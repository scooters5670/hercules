<item>
<title>[COLOR white] + [/COLOR] [COLOR orange] Entrar a Hercules [/COLOR][COLOR gold]Solicita tu clave en telegram.[/COLOR]</title>
<link>NA</link>
<externallink>$doregex[getinput]</externallink>
<thumbnail></thumbnail>
<fanart></fanart> 
<regex>
<name>getinput</name>
 
<expres><![CDATA[#$pyFunction
import xbmcgui,xbmc, xbmcaddon, time, base64,
 
def GetLSProData(page_data,Cookie_Jar,m,txt='Contraseña'):
    addon = xbmcaddon.Addon('plugin.video.Hercules')
    addon_version = addon.getAddonInfo('version')
    profile = xbmc.translatePath(addon.getAddonInfo('profile').decode('utf-8'))
    solver = InputWindow( txt=txt)
	verify = 'bmV0YWk='.decode('base64')
    solution = solver.get()
    if solution == 'verify':
    return "https://pastebin.com/raw/NBn5yngf"
   
 
    class InputWindow(xbmcgui.WindowDialog):
    def init(self, *args, **kwargs):
 
        self.txt = kwargs.get('txt','')
 
        xbmcgui.WindowXMLDialog.init(self)
        #self.addControl(self.txt)
        path = xbmcaddon.Addon().getAddonInfo('path')
        self.kbd = xbmc.Keyboard()
        self.kbd.setHeading( self.txt)
 
    def get(self):
        self.show()
        time.sleep(2)
        self.kbd.doModal()
        if (self.kbd.isConfirmed()):
            text = self.kbd.getText()
            self.close()
            return text
        return False
        self.close()
]]></expres>
<page></page>
</regex>
</item>